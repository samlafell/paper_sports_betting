# backend/database/migrations/  (Empty directory for now)
# You'll use Alembic to generate migration scripts.
# Example:
# alembic init migrations
# alembic revision -m "Create users and bets tables" --autogenerate
# alembic upgrade head
# This is just a placeholder.  Alembic will create the actual files. 