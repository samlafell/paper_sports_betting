// frontend/lib/utils.ts
// Utility functions

export const formatCurrency = (amount: number) => {
  return amount.toFixed(2); // Example: Format to 2 decimal places
};

// Add other utility functions as needed 